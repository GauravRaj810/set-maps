// Array like objects 
/*  - which comprises of length property 
    - Also which we can access by length property and also we can acces by indexing example - string 
    - We can also called string as a array like object 
 */
   const objCheck = 'sakib';
   console.log(objCheck.length);
   console.log(objCheck[3]);       /* it can be acciseble by length  */
